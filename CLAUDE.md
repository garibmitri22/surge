# START HERE — Surge

**This file loads automatically at the start of every session. Read it first, every time.**

## What this project is
**Surge** is an **AI Workforce Platform** — businesses hire AI employees instead of humans ($299/mo per employee). Built by Mitri. Next.js app at `C:\Users\Stephanie\surge`, dev server on port **3001**.

> Note: This is a SEPARATE project from **QC Pro / QC Command Center** (the refinery QC SaaS in `C:\Users\Stephanie\Documents`). Do not confuse them. The old "Houston pool marketplace" idea is SCRAPPED.

## Session start protocol — do this before anything else
0. Read **`FOUNDER-OS.md`** FIRST — the portable personal layer (who Mitri is, how we work, the cross-project vision, standing principles). It spans ALL of Mitri's projects, not just Surge. Its permanent home is a dedicated `founder-os` folder; keep this same pointer in EVERY project's CLAUDE.md so the co-founder is never lost when Mitri switches projects.
1. Read **`NORTH-STAR.md`** — what truly separates Surge (memory moat · real actions · vertical depth · outcomes) and the FILTER every move must pass. This sits at the front of every decision: don't let polish jump ahead of the four real separators, and never ship the "toy/wrapper" failure modes.
2. Read **`MEMORY.md`** — the single source of truth for what's built, what's not, and the next steps.
3. Read **`CEO.md`** — the strategy, priorities, and how to communicate (where we are → what's working → what's at risk → next 3 moves).
4. Only then respond. Never recap Surge from memory or old chat logs — always read these files first.

## The filter on every move (from NORTH-STAR.md)
Before building/shipping/pitching, ask: does this (a) deepen the memory moat, (b) extend a real action/integration, (c) add vertical depth, or (d) prove a real outcome? If only brand/polish → fine in small doses, never ahead of the four. Outcomes over activity. Real or transparently-estimated numbers only — never invented.

## How we work
- **Cowork (here)** = project manager: strategy, planning, memory, decisions, writing the Claude Code prompts.
- **Claude Code** = the developer: writes and edits the actual code.

## Keep memory current — save AS YOU GO, automatically
Update **`MEMORY.md`** immediately whenever something memory-worthy happens — a decision, a build handoff, a bug found, a verification result, a strategy change. Do NOT wait for Mitri to say "save" and do NOT wait for session end (sessions can die without warning). Mitri should never need to think about saving — it is always already done. This is the rule that stops us from ever getting lost again.

---

<!-- Next.js build rules below -->
@AGENTS.md
