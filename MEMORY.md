# Surge — Project Memory

## What It Is
**Surge** is an AI Workforce Platform. Businesses hire AI employees instead of traditional hires.
- Mission: Build the operating system for AI workers
- A business owner logs in and manages a team of AI employees that work 24/7
- Pricing: **$399/mo single employee · $999/mo team (Aria+Nova+Opus, run by Atlas)** — centralized in `lib/pricing.mjs` (the old scattered "$299" literals caused a wrong-price bug; fixed June 5). Human anchor: $50K/yr.
- Goal: Get product-ready ASAP, built by Mitri for himself first, then sell to others

## The Pivot
Surge was previously an AI home improvement marketplace (Houston pools). That version is scrapped. The new direction is fully AI Workforce Platform.

## Tech Stack
- **Framework:** Next.js 16 (App Router) + TypeScript
- **Styling:** Tailwind CSS 4 + inline styles (light theme)
- **Backend/DB:** Supabase (PostgreSQL) — ✅ WIRED IN (Step 1 done June 3, see below)
- **Auth:** Supabase Auth — 🔨 IN PROGRESS (Step 2, see below)
- **Project folder:** `C:\Users\Stephanie\surge`
- **Dev server port:** 3001 (3000 is used by QC Pro)

## Design Language
- **LIGHT theme only** — `#f9fafb` background, white (`#ffffff`) cards, dark text (`#111827`). NOT dark. (The old "#08090d dark mode" note was wrong — do not reintroduce it.)
- Apple × Palantir × Tesla feel
- Accent color: `#6366f1` (indigo)
- Geist Sans + Geist Mono fonts
- Animated status dots, score rings, fade-in transitions
- CSS variables in `globals.css`

## What's Been Built (June 2026)

### Foundation ✅
- `lib/mockData.ts` — all mock data: employees, tasks, activity log, memory entries, dashboard stats
- `lib/company.ts` — company profile helpers (localStorage until Supabase)
- `app/globals.css` — light theme, CSS variables, animations
- `app/layout.tsx` — root layout using AppShell
- `components/AppShell.tsx` — hides sidebar on onboarding/landing pages
- `components/Sidebar.tsx` — nav with Dashboard / Workforce / Tasks / Memory / Settings + Hire CTA + onboarding gate
- `app/page.tsx` — redirects to /dashboard

### Pages ✅
- `app/onboarding/page.tsx` — 7-question interview: company name, industry, target customers, brand tone, goal, competitors, team size. Stores to localStorage. Redirects to dashboard on complete. Left panel shows progress steps. Animated transitions.
- `app/dashboard/page.tsx` — HQ dashboard: personalized greeting with company name, live ticker, Workforce Score + breakdown bars, 6 metric cards, active tasks quick view, workforce status panel (clickable), activity feed
- `app/workforce/page.tsx` — employee cards (Aria, Nova, Opus) with score rings, KPIs, status dots + "In Development" pipeline (Rex, Clara, Evan, Piper, Finn)
- `app/workforce/[id]/page.tsx` — full employee detail: header, KPIs, tabs (Tasks / Activity / Responsibilities)
- `app/tasks/page.tsx` — task list with stats, filter tabs, assign modal, click-to-cycle status
- `app/memory/page.tsx` — memory entries by type, expandable cards, add modal
- `app/settings/page.tsx` — company profile display, re-run onboarding button, plan info, danger zone

## Workforce Architecture Laws (Mitri, June 3 2026 — apply to EVERY build)
1. **No employee gets bottlenecked.** Every employee follows the same pattern — persona (Layer 1/2) + tool belt + loop + scoreboard — so `aria.md` is the template, never a special case. Adding employee #4, #10, #50 must never require rebuilding anything.
2. **Shared company brain.** All employees read the same memory layer (ICP, brand voice, SOPs, rapport). Learn once, everyone knows it.
3. **Handoffs are first-class.** Any employee can pass work to any other (Aria → Opus: "prep the onboarding docs for the meeting I booked"). Tasks can be assigned to ANY employee; handoffs show in the activity feed.
4. **Company cohesion is the end state.** Eventually the workforce operates as a coordinated company — employees seeing each other's relevant work, coordinating on shared goals, reporting as a team in the CEO briefing. Every data structure (tasks, conversations, activity, memory) must be multi-employee from day one so cohesion is additive, not a rewrite.

## AI Employees

> **Persona system (June 3, 2026):** Each AI employee gets a persona doc in `personas/` — Layer 1 = core identity (ships with product), Layer 2 = per-company context injected from the memory layer. These become runtime system prompts when real AI is wired in. **`personas/aria.md` is DONE** (written by Mitri + CEO) and is the template for Nova, Opus, and all future employees.
> **Aria operating spec (approved June 3, in aria.md):** one brain (Claude only — no GPT split), one memory (Supabase — Surge IS the CRM, `leads` table ours; external CRM sync = enterprise later), tool belt registry (v1: research_web, create_lead, draft_email approval-mode, log_activity, report; v2: send_email, book_meeting — direct APIs, Zapier edges only, Figma cut), standing daily loop + triggers + commanded work, results feed Workforce Score.
> **Lead Lifeline system (in aria.md):** law = no lead without next_action + next_action_at; heartbeat sweeps overdue actions; SLAs (reply within hours, day 3/7/14 cadence, 90-day recycle); stale leads in morning check-in + dashboard red flag.
> **Aria's voice:** living character — signature style, opinions earned from real data only, rapport continuity, stays the sales voice as other agents join.
> **Aria tradecraft (CEO additions June 3, in aria.md):** objection playbook (new-category objections + grows from real conversations), lead scoring rubric (0–100: ICP fit 40/pain 30/ability 20/reachability 10, reasons required), deliverability hygiene (domain warmup, caps, spam triggers, bounce limits — the silent killer), handoff briefs (auto one-pager per booked meeting), disqualification discipline, experimentation method (one variable, real samples).
> **Aria capability roadmap (in aria.md):** Phase 1 = written outreach (now). Phase 2 = voice calls — qualification/booking (trigger: Phase 1 books meetings consistently, not a date). Phase 3 = full-cycle closing on low-ticket deals. Engineering rule: channel-agnostic data model + one persona brain across all channels, so adding voice never requires a rebuild.

### Aria — Sales Representative
- Color: `#a78bfa` (purple)
- KPIs: Leads Found, Emails Sent, Replies, Meetings Booked, Pipeline Value
- Personality: Sharp, direct, persuasive — top-performing SDR

### Nova — Marketing Director
- Color: `#34d399` (green)
- KPIs: Monthly Traffic, Leads Generated, Content Published, Conversion Rate
- Personality: Creative, strategic, data-informed

### Opus — Operations Assistant
- Color: `#60a5fa` (blue)
- KPIs: Tasks Completed, Hours Saved, Projects Managed
- Personality: Precise, systematic, thorough

## Pipeline Employees (In Development)
Rex (Recruiter), Clara (Customer Service), Evan (Executive Assistant), Piper (Project Manager), Finn (Finance Manager)
- **Gap logged June 4 (validated by Mitri's own usage):** no brainstorm/ideas employee exists — Opus honestly flagged it when asked "who do I brainstorm with?" Original decision: pipeline card only, measure demand, don't build before revenue (90-day rule). Side note: Opus gave this honest answer with NO persona file yet — honesty guardrail holding.
- **⤴️ REVISED June 4 (Mitri, CEO concurs): the Strategist/Chief-of-Staff IS the next employee build.**
- **✅ NAMED & PERSONA WRITTEN June 4: ATLAS — Chief of Staff.** (Name: fits the elevated-noun family Aria/Nova/Opus; carries the whole map. "Sage" rejected — Sage Group accounting owns that word in our SMB market.) **`personas/atlas.md` is DONE** (CEO + Mitri) — Mitri's mandate: most important employee so far, backbone + leader of the team, modeled on the best CoS ever (Agrippa, Hamilton, Hopkins, Baker III, Sandberg/Shotwell), unique voice (calm gravity, morning brief, one challenge per conversation, zero ego), powerful memory (decision log / open-loop ledger / idea-to-work pipeline). His KPI: daily owner conversations. KEY ENGINEERING NOTE in the file: Atlas's chat context must see the WHOLE board (all employees' tasks, pipeline, KPIs, full memory) — more context than any other employee — plus create_task assignable to ANY employee. Dev build = atlas.md exists; needs: employees-table row (id 'atlas'), expanded context injection, roster/UI card. Does NOT displace Aria email channel as dev priority.
- **✅ June 4 (later): BOTH builds CODE-COMPLETE by dev** — usage limits + Atlas. `lib/chat-prompt.mjs` now has Chief-of-Staff mode: `isChiefOfStaff` ctx flag → WHOLE-BOARD VIEW block (all employees' open tasks, lead pipeline summary, per-employee activity snapshot), rule 6 (Morning Brief ritual, route via create_task `assignee`, memory `kind` tags decision/open-loop/idea). create_task now takes `assignee` (any employee); remember_detail takes `kind`. **VERIFICATION PENDING:** dev must run migrations + `verify-usage-limits.mjs` + `verify-atlas.mjs` green, then commit + push. Dashboard/Workforce render Atlas from DB automatically.
- **🎯 LANDING + PACKAGING DECISIONS (CEO, June 4): Atlas included in EVERY plan** (COGS ~$20-30/mo chat-only; he's the retention engine + organic upsell — routes work to not-yet-hired employees: "that's Nova's lane, want to hire her?"). Landing: team section → 2×2 grid with Atlas, positioning line "Your team comes with a Chief of Staff." Team plan story: $999 = FOUR employees ($250/employee) run by a Chief of Staff. Reason the rule changed: HQ Chat v1 + personas + memory + create_task now exist, so his marginal cost collapsed to a persona file + DB row (half-day) — his tool IS the conversation. Strategic role: the DAILY-touch retention employee (briefing is weekly, score is passive; he makes Surge a daily habit) + every brainstorm feeds the memory layer (moat compounds) + his ideas route into tasks for other employees (drives team usage → sells the $999 plan). HARD CONDITIONS: (1) runs on total company context (tasks/leads/KPIs/memory/roster) — generic advice = failure point #1; (2) ideas must become work ("hand this to Aria as a task?"); (3) does NOT touch the critical path — Aria's email channel stays the dev priority; Strategist is parallel (persona via aria.md template). OPEN: his name + exact lane before persona is written. His KPI: daily conversations with the owner.

### Additional Pages ✅
- `app/briefing/page.tsx` — Weekly CEO Briefing: score banner, accomplishments, attention items, planned work. Linked in sidebar.
- `app/landing/page.tsx` — Public marketing page at /landing: hero, employee showcase, how it works, FAQ, email CTA. No sidebar.
- `app/workforce/page.tsx` — Updated with Hire modal: role selector, $299/mo pricing, confirmation animation.

## ✅ Step 1 DONE — Supabase wired in (June 3, 2026)
Mock data replaced with a live Supabase database.
- **Supabase project:** ref `lnjvwddxytlpxukdnphp`, URL `https://lnjvwddxytlpxukdnphp.supabase.co`
- Publishable (anon) key in `.env.local` (NOT committed). Secret/service key + DB password NOT stored anywhere in repo.
- Built: `lib/supabase.ts` (client), `lib/data.ts` (all data access), `supabase/schema.sql` (tables: companies, employees, tasks, activity_log, memory_entries + seed data), `lib/company.ts` rewritten to use DB instead of localStorage.
- All pages (dashboard, workforce, workforce/[id], tasks, memory, settings, onboarding, briefing) now pull from `lib/data.ts`. `lib/mockData.ts` kept for TypeScript types only.
- ⚠️ **RLS is DISABLED on all tables** — must be locked down when Auth (Step 2) lands. Do not forget.
- Verified in repo by Cowork PM June 3. Final user check: task survives page refresh.

## ✅ Step 2 DONE & PROVEN — Auth + multi-tenant RLS lockdown (June 3, 2026)
- verify-auth.mjs → ALL CHECKS PASSED (migration applied, RLS blocks anon reads=0 rows + writes=401).
- Two-tenant isolation proven with real accounts: Tenant C sees ZERO of Tenant B's tasks/companies. "ISOLATION PROVEN."
- proxy.ts route protection live (Next 16 renamed middleware→proxy). Data layer scoped by company_id. mailer_autoconfirm flipped ON (email confirmation OFF for testing).
- ⚠️ TODO before public launch: re-enable "Confirm email".
- ✅ June 4: yesterday's sbp_ token REVOKED by Mitri. A new one issued to the dev for the v2 build window. **Token policy (June 4):** management tokens are build-time only (migrations/admin) — Aria's runtime never uses them (she runs on project URL + anon key + Anthropic key). Rule: new token per build session, REVOKE when that build's verification passes, shortest expiry available. Verified June 4: token not in repo, git history, or .env.local.
- ✅ June 4 resolved: ANTHROPIC_API_KEY was missing from .env.local because the old key was deleted during the token cleanup. Fix in motion: Mitri creates a new key (named "Surge", with billing credit) and adds it to .env.local HIMSELF (never pasted in chat — auto-flag risk), dev restarts + tests Aria. Production rule: key goes in host env vars at deploy, never in files or chat.
- Real accounts in system: garibmitri1@gmail.com, mitri.garib@yahoo.com. Seeded demo data may need claiming to main account if dashboard looks empty.

## (historical) Step 2 handoff notes
- Claude Code prompt handed off: @supabase/ssr cookie auth, middleware route protection, /login + /signup pages (light theme — app is light, not dark), sign out in sidebar/settings, `supabase/auth_migration.sql` (user_id on companies, company_id on tasks/activity_log/memory_entries, RLS ON for all tables, employees = shared read-only catalog, snippet to claim seeded rows), lib/data.ts + lib/company.ts scoped per company, empty states required.
- Mitri's dashboard task: turn OFF "Confirm email" in Supabase Auth settings (re-enable before public launch).
- **VERIFY:** signup→onboarding→dashboard flow works; signed-out users redirected; a SECOND account sees only its own empty workspace (= RLS proven).
- Then Step 3: onboarding interview feeds the memory layer (the moat).

## ✅ Step 3 BUILT — HQ Chat v1 LIVE (June 3, 2026) — pending Mitri's feel-test
Dev verified: chat tables + RLS passing (verify-chat.mjs), ANTHROPIC_API_KEY valid (claude-sonnet-4-6), /api/chat auth-gated, build green. Features live: streaming in-character chat on employee detail pages (Chat = default tab), real-data-only reporting (honest when empty), command→plan→approve→creates real task, rapport→memory entries (tag 'rapport'), persistence, Ctrl+K command bar with "Aria, ..." routing. v1 scope: she plans + creates tasks, does NOT execute research/outreach yet (HQ Chat v2). NOTE: Nova/Opus have NO persona files yet — they run on DB role/personality only; writing nova.md + opus.md (using aria.md as template) is a cheap high-value next task. Mitri's Anthropic key in .env.local (gitignored).

## 📐 (original design) HQ Chat: "Talk to your team" (June 3, 2026)
The core product feature. Decided by Mitri + CEO:
- **Two doors:** global command bar docked on every page (Ctrl+K, "Aria, ...") + full chat tab on each employee detail page.
- **Persistent conversations** in Supabase (`conversations`, `messages` — channel-agnostic per Aria roadmap rule).
- **Brain:** Claude API + persona Layer 1/2 (`personas/*.md`) + memory layer + LIVE task/KPI data. Hard rule: progress reports come from real DB data — never invented.
- **Rapport memory:** personal details the owner shares are stored as memory entries (tagged 'rapport') and recalled in later chats — humanizes employees, deepens the moat.
- **Commands → tasks:** a directive (e.g. "rank and qualify top 100 Houston med spas, book sales calls, report back") → she proposes a plan → owner approves → structured task on Tasks board → progress in activity feed → report delivered in chat.
- **Proactive engine:** morning check-in, decision-needed alerts, end-of-day report.
- **Approval mode guardrail:** external actions (sending email, booking) require owner approval until flipped to autonomous per-employee.
- **Phases:** v1 chat+tasks+real-data reports (build right after Step 2 auth verifies) → v2 first real action: live web research/lead qualification → v3 email+calendar integrations for sending/booking.

## What's NOT Built Yet
- HQ Chat v2 — Aria EXECUTES (live web research, lead qualification, draft outreach w/ approval) + `leads` table + Lead Lifeline system
- Persona files for Nova + Opus (use aria.md as template — cheap, high value)
- Weekly CEO briefing email (Monday automation)
- Billing / payments (Stripe)
- Mobile pass on landing page (dev offered, not run yet)
- Domain + real email (Contact sales mailto → garibmitri1@gmail.com for now)
- Before public launch: re-enable "Confirm email" in Supabase

## Session Log — June 3, 2026 (one day, foundation → real product)
✅ Supabase wired (Step 1) → ✅ Auth + multi-tenant RLS proven (Step 2) → ✅ HQ Chat v1 live (Step 3, pending Mitri feel-test: voice / command-loop / rapport) → ✅ v0 landing integrated honestly → ✅ aria.md complete (identity, operating spec, Lead Lifeline, voice, tradecraft, roadmap) → ✅ Workforce Architecture Laws → ✅ daily 8am meeting scheduled → ✅ CEO.md upgraded (skill stack + verify-yourself rule). NEXT SESSION OPENS WITH: Mitri's HQ Chat feel-test results → tune aria.md if needed → then HQ Chat v2 (her first real action).

## Session Log — June 4, 2026
✅ **HQ Chat feel-test PASSED** (Mitri ran it with Aria): voice is sharp and honest, zero fabricated capability claims, plan→approve→task loop worked, smart vertical reasoning. Verdict: do NOT tune aria.md — go straight to v2.
⚠️ **Feel-test exposed the v1/v2 gap:** Aria created an approved prospecting task and promised "I'll check back in when I have the ranked list" — but v1 has no execution engine. The task sits idle; trust decays daily. **HQ Chat v2 is now the single priority.**
🎯 **GTM DECISION (Mitri, June 4): Surge is its own first customer.** Aria's job is to sell Surge. ICP: **med spas (first), real estate teams, gyms/fitness studios** — owner-operated, 1–20 employees, obvious follow-up/hiring pain. Geography: **North Houston up to Conroe** (tight region, fast iteration). Old "B2B SaaS" ICP framing superseded for outreach v1.
📋 Live approved task on Aria's board: identify + score top 50 prospects across the 3 verticals, rank by pain signals, draft per-vertical cold email sequences, stage for send. v2 must execute exactly this.
🚧 Sending still blocked on domain + real email (deliverability hygiene per aria.md) — v2 = research + score + drafts (approval mode), NOT sending.
🎬 **Nova scope decision (Mitri, June 4): Nova = FULL-STACK marketing** — written content + social media + video. Phase 1 (now): posts, scripts, production-ready UGC briefs in chat. Phase 2: social publishing integration (approval mode). Phase 3: AI video generation via API — **Higgsfield designated candidate tool** ($15–129/mo credit plans, API on higher tiers; video models burn 6–70 credits each — evaluate unit economics before wiring). No integration built now — roadmap text in nova.md only.
📤 Two prompts handed off June 4: `prompts/hq-chat-v2-prompt.md` (critical path) + `prompts/personas-nova-opus-prompt.md` (independent).
🔨 **June 4 PM repo check (Cowork CEO, verified directly): BOTH builds are code-complete in the repo** — v2: `supabase/leads_migration.sql` (leads + lead_drafts + next_action law), `app/api/agent/run/route.ts` (execution engine + tool belt), `app/leads/page.tsx`, `lib/leads.ts`, `scripts/verify-leads.mjs`. Personas: `personas/nova.md` + `personas/opus.md` exist. **NOT YET VERIFIED** — blocked because ANTHROPIC_API_KEY is still missing from .env.local (Mitri adding new key himself via Notepad). Verification gate: dev runs verify-leads.mjs + Aria's real med spa run (lead count, top 5 + scores) + personas chat test.
🐛 **Fix queued (June 4): employees can't see their teammates.** Aria told Mitri she has "no visibility into who else has been built" — violates Architecture Laws #3/#4 (handoffs/cohesion). Fix: inject live employee roster (name, role, lane, status) from the employees DB table into every chat's system context. Handed to dev. ✅ Counterpoint proven same exchange: shared memory layer WORKS — Aria cited the strategist gap logged via Opus.
⚠️ **GIT RISK (June 4): repo has ONE commit ever** ("Initial Surge project setup") — all June 3–4 work (auth, chat, v2, personas) is uncommitted. Dev must commit everything as part of verification. Going forward: commit at every verified milestone.
🔑 Token cleanup done: old Anthropic keys to be deleted (one key per purpose policy); today's sbp_ token revoked AFTER v2 verification passes.
🐛 **Fixed June 4 (Cowork, verified live): HQ Chat falsely claimed "research_web isn't connected — talk to Mitri."** Root cause: chat prompt let Aria frame the v1 chat scope as a missing product integration. Fix: HARD RULES 3+5 rewritten — directives route to create_task + "hit Run on the Tasks page"; claiming a missing/not-connected integration is banned. Verified live against claude-sonnet-4-6 (plan→approve→create_task→points to Run, forbidden-phrase scan clean). **Refactor:** prompt + chat tools extracted to `lib/chat-prompt.mjs`, imported by BOTH `app/api/chat/route.ts` and `scripts/verify-chat-prompt.mjs` — test can no longer drift from production. Note: stray `app/api/chat/route.ts.clean` may exist (sandbox couldn't delete) — safe to delete.
🔚 **Session ended June 4 with ANTHROPIC_API_KEY added by Mitri; execution test NOT yet confirmed.** NEXT SESSION OPENS WITH: results of Aria's first real run — the 7-step test script given to Mitri (liveness → "start working the med spa list" → spot-check 3 leads are REAL businesses → score reasons → draft quality → report counts match /leads → Nova social+UGC test). Then: dev commits everything to git, sbp_ token revoked, roster-visibility fix verified. If the run produced real leads, Surge did its first real day of work — next milestone after that is domain + email setup so approved drafts can actually send.

## 📝 MITRI'S NOTES — June 5, 2026 (brainstorm dump, captured by CEO)
1. **Tenant isolation question — ANSWERED, no action:** memory/brains are scoped per ACCOUNT (auth user → company_id via RLS), never by company name. Two-tenant isolation proven June 3. Atlas + team restart fresh per account; no cross-company mix-up is possible while RLS holds. Company name is display-only.
2. **🎙️ VOICE MODE (talk to the team instead of typing)** — Mitri wants two-way voice chat with all employees eventually. Roadmap item, not specced. Cheap v1 path when ready: browser speech-to-text input + TTS on replies; real-time voice later.
3. **📞 ARIA PHONE CALLS** — Mitri wants realistic AI phone calls that handle rejection live (like other AI SDR companies). Already in aria.md as **Phase 2** (trigger: Phase 1 books meetings consistently — not a date). Note: live-call rebuttals will need a voice-latency stack (e.g. Vapi/Retell-class) — evaluate when Phase 2 triggers.
4. **Objection handling status — ANSWERED:** yes, aria.md has an objection playbook (new-category objections + grows from real conversations) — built for WRITTEN outreach today; phone rebuttals arrive with Phase 2 voice.
5. ✅ **FIXED LIVE (Cowork, June 5): stale $299 pricing in 4 places** — Sidebar "Hire an Employee" card, workforce hire modal (header + monthly total), and **`app/api/agent/run/route.ts` cold-email prompt (Aria would have quoted $299 to real prospects)**. All now $399/agent + $999 team framing. NOT yet committed — dev sweeps into next commit. Lesson: pricing constants are scattered — dev should centralize price in ONE module (config) so this can't drift again.
6. **🗣️ VOICE QUALITY FLAG: too many em-dashes ("—") in employee replies = AI-sounding, too generic.** Mitri reads heavy dash usage as a tell that answers are template-ish rather than specific. Action: add to persona layer/style rules (write like a person: fewer dashes, shorter sentences, concrete specifics from the company brain).
7. **🧠 ATLAS INTERVIEW DEPTH:** Mitri doesn't want Atlas asking the SAME stock questions every time — he should work through problems and ask the right question for the moment (use the board, memory, and the last answer to pick the next question; never re-ask what the brain already knows). Persona/prompt upgrade for atlas.md — queue for dev.
8. 📤 **Build prompt handed off June 5: `prompts/polish-pass-june5-prompt.md`** — sweeps Cowork's uncommitted fixes (memory page + 4 price spots), centralizes pricing in lib/pricing.ts, intake profile UPSERT (dedupe Offer), dashboard+briefing honest empty states (HARD GATE before billing — kills fake $184K/86 score), shared em-dash/style rule in chat-prompt.mjs with verify check, Atlas contextual-questioning rules with contradiction-surfacing verify, ICP reconciliation surfaced via Atlas brief. Email channel still outranks it the moment domain credentials land.
9. **🔬 RESEARCH ENGINE LOOSENED (Mitri's call, June 5 — added to polish-pass prompt item 6):** the onboarding site-scan was one-strike (even an EMPTY result cached forever → wrong first URL = permanently blind Atlas) and search-only (can't see unindexed sites). New params: thin scans don't consume the cap, 3 successful scans/company (new URL = fresh scan, same URL = cache), direct page fetch BEFORE web_search, hard daily backstop stays, all runs logged. Cost worst case ~$0.30/customer (Haiku ≈ 5–10¢/scan). Also: Atlas must never invent internals when a scan is thin (no "pre-session crawl" lore) — plain "scan came up short."

## 🌐 DOMAINS — June 5, 2026 (availability checked live on Namecheap by CEO)
- All clean "surge" .coms are premium resales: surgehq.com $2,895, usesurge.com $1,850, getsurge.com $12.3K, trysurge.com $9K. hiresurge/surgeteam/surgework/surgehq.ai taken. Buyable later as a brand upgrade if revenue justifies.
- **DECISION (Mitri approved): main domain = surgehq.io ($34.98/yr), Aria's sending domain = getsurgehq.com ($11.28/yr, NEWCOM679 promo → $6.79 if applied logged-in).** Matches "Surge HQ" product language; .com sender reads legit in inboxes. Cart staged by CEO (hosting upsell removed, free privacy ON, auto-renew ON deliberately — expired sending domain mid-campaign = disaster); Mitri completes account + payment himself.
- Namecheap account: username **SurgeHQ** (garibmitri1@gmail.com). Password NOT stored anywhere per the standing secrets policy (June 4: secrets never in repo files or chat) — it lives in Mitri's browser password manager. CEO advised: rotate it when convenient + enable 2FA (domains = hijack target).
- ✅ **June 5: GOOGLE WORKSPACE LIVE on getsurgehq.com** — account created by Mitri (admin **mitri@getsurgehq.com**, Business Starter, created under a NEW Google account, NOT Stephanie's personal Gmail), domain VERIFIED via TXT record (Cowork planted it in Namecheap Advanced DNS), Gmail MX set via Namecheap's one-click Gmail preset ("Gmail automatically configured"), activation confirmed — Google processing. **DECISION: aria@ = free ALIAS on Mitri's user, NOT a $16.80/mo second seat** (sending goes through Resend; alias receives replies; saves ~$200/yr until reply automation needs a real mailbox). ✅ **aria@getsurgehq.com alias ADDED** (free, on Mitri's user). ✅ **GMAIL ACTIVATED June 5** — first attempt failed because Namecheap's "Gmail" preset plants Google's LEGACY 5-record MX set while the new activation flow requires exactly ONE record (smtp.google.com, priority 1); Cowork switched MAIL SETTINGS to Custom MX with the single record → retry passed immediately. (Lesson for any future domain: skip the Namecheap Gmail preset, use Custom MX → smtp.google.com/1.) ✅ **RESEND SET UP June 5 (account: Google login as mitri@getsurgehq.com, workspace "getsurgehq")** — domain getsurgehq.com added (us-east-1), Cowork planted ALL records in Namecheap Advanced DNS: DKIM TXT (resend._domainkey → p=MIGf...), SPF TXT (send → v=spf1 include:amazonses.com ~all), MX (send → feedback-smtp.us-east-1.amazonses.com, prio 10), DMARC TXT (_dmarc → v=DMARC1; p=none;). Verification triggered — Resend status Pending, auto-flips to Verified on propagation (minutes–hours). NOTE: root MX (@→smtp.google.com) untouched — Resend rides the `send` subdomain, so Gmail receiving + Resend sending coexist cleanly. ✅ API key created (name "Surge Aria Sending", Sending-access only) + in .env.local. ✅ Dev WIRED send_email June 5 (approve→send via deliverDraft, warmup drip in heartbeat 10/day→~100/day31, suppression+CAN-SPAM gates, verify-email green, pushed 6d8e6f8). ✅ **email_migration.sql RUN by Cowork June 5 ("Success. No rows returned")** — email_sends/email_suppressions/unsubscribe fn/physical_address live. **REMAINING GATES TO FIRST SEND: (1) Mitri sets physical_address in Settings → Sending & Compliance — **DECIDED June 5 (Mitri): VIRTUAL ADDRESS** — no home address in email footers. iPostal1 ($9.99/mo), location picked by CEO: **10160 Highway 242, Ste 800, Conroe, TX 77385** (The Woodlands/Conroe area — notary ONSITE for the Form 1583, international shipping, $2 local pickup). Mitri completing signup + payment + USPS Form 1583 notarization. When done: paste address into Surge → Settings → Sending & Compliance → live send test → warmup starts.
   ✅ **RESEND DOMAIN VERIFIED June 5, 2:07 PM** ("Domain verified: Your domain is ready to send emails" — added 1:40, DNS verified 2:06, verified 2:07). Gate 3 CLOSED.
   🚀 **FIRST EMAIL EVER SENT — June 5, 2026, 3:50 PM.** Full pipeline live test: Cowork set physical_address via SQL (iPostal1 address purchased, Business Green $14.99/mo — ⚠️ Form 1583 notarization still PENDING, Mitri doing it in person), staged test lead+draft (score-100 lead "Surge Live Send Test"), approved via the REAL Leads-page Approve&send button → server sent through every gate (warmup cap, suppression, CAN-SPAM footer + unsubscribe). email_sends: status **sent**, provider_id cb94566c-491b-4ebb-9baf-66637fdcb070, to garibmitri1@gmail.com. **WARMUP CLOCK STARTED.** ✅ CONFIRMED IN MITRI'S INBOX ("arias first email" — June 5). The CRM also proved itself: overdue next_action flagged red on /leads automatically. Cleanup note: test lead/draft can be deleted after confirmation, or kept as send #1 of warmup day 1. (2) Resend domain verification still Pending (auto-flips).** Then: live send test to an address Mitri picks → warmup ticking. CEO call on dev's cron question: **nightly cron YES once live** — warmup requires DAILY consistency; per-visit heartbeat skips days and stalls reputation. Approval still gates content (only pre-approved drafts drain). Optional later: Google's "Authenticate outgoing emails" (DKIM for Gmail-sent replies).
- ✅ **PURCHASED June 5, 2026 (order 204465530, 11:01 AM): Surge owns surgehq.io (main) + getsurgehq.com (sending).** Auto-renew + free privacy ON. THE EMAIL CLOCK HAS STARTED. Remaining before dev handoff: (1) Google Workspace mailbox aria@getsurgehq.com (~$7/mo), (2) Resend account + API key on getsurgehq.com, (3) hand dev DNS access + Resend key → SPF/DKIM/DMARC + warmup same day (10–20/day, scale 2–4 wks per aria.md). Also unblocked: swap landing CONTACT_SALES_MAILTO placeholder to a real address; deploy target surgehq.io when hosting day comes.

## 🚀 ARIA'S FIRST REAL RUN — June 5, 2026 (THE milestone)
**"Tasks not running" bug (Mitri) → root cause: `hours_migration.sql` never applied** — hours enforcement shipped in code but hours_ledger didn't exist, so every run died at the gate. Cowork ran the migration (all companies granted; Surge plan='team', topped to 200h after the grant fired at 70). **RULE (2nd occurrence in one day): a build that ships a migration is NOT done until the migration RUNS — dev lists pending migrations in every handoff; PM runs them immediately.** (Also: tasks page transiently rendered empty/"Your Company" pre-fix — resolved after migration; 11 tasks were on the board all along, incl. Atlas-routed team tasks + duplicates + a "Delete med spa task from task system" task — task dedupe/cleanup queued for dev.)
**FIRST REAL RUN RESULT (from the actual Tasks-page Run button): 16 REAL leads** — Houston→Conroe med spas with real addresses (Conroe Aesthetics & Wellness 82, Radiant Skin & Health 79, Montrose Med Spa 78, Utopia Plastic Surgery 77, Dream Aesthetics 75…), **7 cold-email drafts pending approval, 5 tasks completed, total cost $0.33** (vs $1.50–2.00 modeled — Haiku routing pays off). 🐛 **DEV FLAG: hours balance still 200.0 after runs — debit isn't posting** (gate enforces, charging doesn't). REMAINING from the 7-step test: Mitri spot-checks 3 leads are real + reads draft quality.
**📦 LAUNCH STATUS (June 5 eve):** ✅ honest dashboard done+verified (launch blocker cleared); ✅ cron GET handler added (Vercel crons are GET — POST-only would have 405'd nightly); ✅ unsub base URL default hardened to surgehq.io (parking-page CAN-SPAM risk closed in code); DEPLOY.md written with env table (6 always: SUPABASE_URL/ANON_KEY, ANTHROPIC_API_KEY, RESEND_API_KEY, EMAIL_FROM, NEXT_PUBLIC_APP_URL=https://surgehq.io + 2 cron: CRON_SECRET, SERVICE_ROLE_KEY). **CEO decision: NO --service-role mode in verify scripts** (token policy + an RLS-bypassing suite can't catch RLS regressions); instead SEQUENCE: full prod verify BEFORE Confirm-email flips, structural/read-only after. REMAINING TO LAUNCH: Mitri's Vercel account + env paste → Cowork DNS → dev prod verify → Confirm-email flip → freeze lifts.
🚀 **DEPLOYED LIVE June 5, 2026.** Vercel project **surge-hq** (team garibmitri1-9581, Hobby/free), GitHub app installed on garibmitri22/surge (auto-deploys every push to main), 6 env vars set (NEXT_PUBLIC_SUPABASE_URL/ANON_KEY, NEXT_PUBLIC_APP_URL=https://surgehq.io, EMAIL_FROM, ANTHROPIC_API_KEY, RESEND_API_KEY). Build green, first deploy live at surge-hq-nine.vercel.app. **DNS: surgehq.io wired in Namecheap** — parking records (CNAME www, URL redirect @) deleted; added A @ → 216.198.79.1 + CNAME www → cname.vercel-dns.com. DNS resolved instantly (dns.google confirms surgehq.io → 216.198.79.1); Vercel validated (blue check) → SSL cert generating. **surgehq.io about to be live worldwide.** vercel.json cron present but dormant (needs CRON_SECRET + SERVICE_ROLE_KEY in Vercel env — not yet added; Level-4 heartbeat waits on those). ✅ **CONFIRMED LIVE: https://surgehq.io/landing loads over real HTTPS** (valid SSL, served from Vercel) — "Hire your AI workforce. $399/mo. Your team comes with a Chief of Staff." Correct pricing, honest copy. SURGE IS PUBLIC ON THE INTERNET (June 5, 2026). REMAINING before send-freeze lifts: dev runs prod verify suite against https://surgehq.io → re-enable Confirm email in Supabase (pre-launch gate) → then the 7 drafts can send. Also pending (non-blocking): CRON_SECRET + SUPABASE_SERVICE_ROLE_KEY in Vercel env to wake the Level-4 heartbeat; landing CONTACT_SALES_MAILTO swap to a real address.
**🛑 SEND FREEZE (Mitri's call, June 5, CEO concurs + escalates): NO outreach to real prospects until the website is LIVE.** Two reasons: (1) prospects Google the sender — parking page = dead on arrival; (2) WORSE: unsubscribe links in every email point to getsurgehq.com (NEXT_PUBLIC_APP_URL default) which is currently a PARKING PAGE — non-working unsubscribe = CAN-SPAM violation. LAUNCH ORDER: dev ships honest dashboard (launch blocker) → Vercel deploy → Cowork points surgehq.io DNS + getsurgehq.com root redirect → set NEXT_PUBLIC_APP_URL to live URL so unsub links resolve → re-enable Confirm email in Supabase → THEN approvals unfreeze. Warmup emails to Mitri's own addresses still fine meanwhile.

## 🎯 PITCH ROTATION BY VERTICAL (CEO analysis, June 5 — Mitri asked "will med spas pay $1K?")
**Answer: yes on economics, no on the current story.** Med spa math: 1 new client ≈ $1.5–3K/yr LTV → $999 pays for itself with ONE extra client/mo; they already spend $1.5–5K/mo on agencies. BUT the bar is proof-in-month-one, not price. **CATCH: Aria's cold email is a B2B weapon; med spa customers are CONSUMERS — cold-emailing consumers = spam (legal + reputation).** Rotated pitch for B2C verticals (med spas, gyms): **SPEED-TO-LEAD + rebooking** — Aria answers every inbound inquiry instantly (5-min response multiplies conversion vs day-later), day-3/7 follow-up, lapsed-client rebooking, Nova content. Same engine + Lead Lifeline laws, pointed at INBOUND. Cold outreach stays the pitch for B2B-ish verticals (real estate teams, agencies, contractors). First 10 prospect conversations = pricing experiment AND pitch experiment. Product implication for roadmap: inbound lead capture (webhook/form/email-in) is the med-spa-facing feature that makes this real — currently leads only enter via Aria's research runs.

## 💰 PRICING & UNIT ECONOMICS — CEO meeting with Mitri, June 4 2026
**COGS verified (live API pricing June 2026):** Sonnet 4.6 $3/$15 per M tokens; heavy Aria task run ≈ $1.50–2.00 (incl. ~30 web searches); chat ≈ pennies (caching already in code). Typical customer ≈ $90/mo COGS → ~70% margin at $299. UNCAPPED 24/7 usage = $6–10K/mo COGS per employee → one customer wipes out thirty. Nova: text/images trivial ($20–50/mo); video is the only real cost — social clips $1–1.50 (fast tiers: Sora 2 $1/clip, Veo Fast/Runway ~$0.15/sec), premium 4K $7.50/clip; hidden killer = regenerations (real cost per ACCEPTED video is 3–5x list). Higgsfield gate confirmed correct: video works metered on fast tiers, breaks us unlimited.
**Market check:** competitors charge far more per AI employee — Artisan $600 entry / $2,400–7,200/mo, 11x $5–10K/mo annual. $299 is underpriced vs category; their buyers are funded B2B teams though, not med spas — our ICP ceiling is lower.
**DECISIONS (Mitri, June 4):**
1. **Restructure pricing: "Hire the Team" flagship at $899–999/mo** (Aria+Nova+Opus, the default on the pricing page); single employee raised to **$399** as entry door; Enterprise custom. At $999/team, $1M ARR = 84 customers (vs 280 at $299). Treat as hypothesis — first 10 prospects are the pricing experiment.
2. ~~**Usage limits are PERMANENT at every price** (not a stopgap): every employee gets a "workday" — 1 autonomous daily cycle + ~60 task runs/mo; Nova video metered in deliverables (8/mo, regens count). Limits invisible to normal users; on hitting cap the employee upsells in character ("add capacity or hire another teammate?").~~ **⤴️ SUPERSEDED June 5 (Mitri): HOURS METERING replaces invisible workday caps.** Hand-tuned per-feature parameters are unknowable and invisible — replaced by ONE visible currency, **branded as HOURS of employee time (Mitri approved; never "credits"/"coins" customer-facing — we sell employees, employees work hours; top-up = OVERTIME).** Chat = UNLIMITED (retention engine, costs pennies; CEO call, Mitri delegated). Work costs hours (scan 0.5h, task run 2h, content 1h, video 12h incl regens; thin/failed = 0 — never charge for our misses). Hour prices DERIVED from usage_log telemetry, pegged to ≥70% margin at full burn, all in lib/pricing.ts. Allowances (hypothesis): Team 200h/mo, Single 70h/mo, no rollover v1. Refills: BOTH overtime packs ($25→20h, $99→100h) AND plan upgrades (Mitri's call). Dashboard fuel-gauge widget ("142h of team time left") + /settings timesheet + in-character low-balance overtime offer via Atlas brief. Design laws from Mitri's own credit frustration: never pay for failures, never ration chat, median customer never sees the gauge (if normal usage hits the wall, the allowance is wrong, not the customer). 📤 **Build prompt: `prompts/coin-metering-prompt.md`** (filename kept, content = hours) — behind email channel, ships WITH Stripe (hours ARE what's billed). Polish-pass research item simplifies: hour cost replaces the bespoke 3-scan cap (keep direct-fetch + no-invented-internals).
3. **Build before first paying customer:** usage counter on /api/agent/run (enforcement) + cost telemetry from day one (tokens + $ per run/employee/company) — future pricing set from real data.
4. Cost discipline: route research/scoring grunt work to Haiku ($1/$5), batch overnight sweeps (50% off), default video to fast tiers (4K = vanity for social).
**📊 THE PLAN (Mitri approved June 4): 20 team customers = the model.** 20 × $999 = $19,980/mo revenue; worst case (ALL maxing limits) COGS ~$5,800 + ~$800 fees/infra → **~$13.4K/mo (~$160K/yr) profit solo**, ~67–70% margin even redlined. Realistic usage → ~$15.5K/mo. Dev contractor is the main cost not in the model (takes it to ~$100–120K/yr). Customers at limit = best customers (max value, renew, refer, upgrade). 20 team customers ≈ wedge math at 2% conversion — reachable from North Houston alone.
**📤 Build prompt handed off: `prompts/usage-limits-telemetry-prompt.md`** — usage quotas on /api/agent/run (workday: 1 daily cycle + 60 runs/mo/employee, config-driven), in-character capacity upsell message, usage_log cost telemetry, Haiku routing, pricing page restructure ($999 team / $399 single), verify-usage-limits.mjs + git commit gate. Ships BEFORE first paying customer.

🐛 **FLAGGED June 4 (CEO, must fix before briefing email ships): `app/briefing/page.tsx` is hardcoded MOCK data** — fake wins ("Booked 28 discovery calls", "Published 31 pieces", "Saved 47 hours") rendered as real. Violates the never-invent-metrics rule on the retention-anchor page itself. Fix: highlights/attention items from real activity_log + KPIs, honest empty states when no data. Not urgent today; HARD GATE before Weekly CEO Briefing email automation. (Also June 4: stray `page_clean.tsx` deleted by Cowork, deletion captured in dev commit accaea4 — resolved.)

## 🧭 NORTH STAR — Agent maturity ladder (Mitri + CEO, June 4)
Levels: 1 talks → 2 acts in chat (tools) → 3 executes multi-step work on command → **4 proactive + reactive (acts on time/event/state triggers without being told)**. **Surge today = Level 3** (Aria's run engine, click-to-run only). **Level 4 = the goal, and it's already on the roadmap as: Lead Lifeline heartbeat + Atlas automated morning brief + Aria standing daily cycle (proactive) + email reply handling (reactive).** It's a scheduler + triggers on the existing engine, not new tech. Disciplines: usage limits ship first (autonomy on a clock = unbounded bill without metering — the "1 daily cycle" quota IS Level 4's budget); approval mode still gates outward actions until owner flips an employee to autonomous.

## 🧹 LANDING HONESTY PASS #2 (CEO, June 4 — after dev shipped Atlas/pricing update)
Dev's structural update PASSED review: 2×2 team grid w/ Atlas ("Included in every plan" badge, amber), headline "Your team comes with a Chief of Staff" + "No competitor ships a team that manages itself", $999 highlighted w/ "$250 each" math, limits framed as "full workday". **BUT fabricated performance stats survived the June 3 honesty pass and were REMOVED by CEO:** fake "847 leads qualified / 156 meetings booked / 34% response / 2,341 tasks / 99.7% accuracy" → replaced with capability stats in Atlas-card style ("Prospecting: Daily", "Every lead scored: 0–100", "Tasks dropped: 0"). FAQ + card copy aligned to approval-mode truth (Aria "you approve before anything sends"; removed Opus schedules/email + "direct integrations" claims; "How it works" step 1 now team-aware). RULE REINFORCED: capability claims yes, invented results never — Atlas's card was the model. ⚠️ Dependency noted: team plan promises "Real-time Workforce Performance Score" — score must be REAL before Stripe billing ships (build order items 6→8). Mobile device pass still not done.

## Build Order (Next Steps) — UPDATED June 4 (Mitri approved; supersedes June 3 order)
**THE STANDING QUALITY BAR (Mitri, June 4): whoever spends $1K of hard-earned cash gets four employees at their A-game, each with a DISTINCT built-in voice — Aria sharp/direct, Nova creative/strategic, Opus precise/systematic, Atlas calm gravity. Any employee that sounds generic or interchangeable fails the bar. Every build and persona change is measured against this.**

1. **Close out the pending verifications** — dev runs migrations, `verify-usage-limits.mjs` + `verify-atlas.mjs` green, landing 2×2 with Atlas on every plan ($999 team / $399 single), commit + PUSH to origin (push still unconfirmed — disk-loss risk open until then).
2. **Aria's first real run confirmed** — the 7-step test (liveness → med spa run → spot-check 3 REAL businesses → score reasons → draft quality → counts match /leads → Nova test). The milestone between us and everything else.
3. ✅ **A-game voice pass DONE (CEO, June 4)** — verdict: dev's nova.md + opus.md already MEET the standard (real reference DNA — Schwartz/Halbert for Nova, Deming/Gawande/Ohno for Opus — distinct voices, tradecraft, guardrails, own equivalent systems). Targeted fixes applied, not a rewrite: (a) **CRITICAL — all personas still sold $299/Basic/Growth pricing; Aria would have quoted the wrong price to real prospects.** All four Layer 2 blocks updated to $999 team (run by Atlas, included every plan) / $399 single / Enterprise custom, anchored vs $50K human. (b) Atlas woven into Nova+Opus (voice distinctness lines, Opus's "whole board" lane collision fixed: Atlas sets priorities + routes, Opus runs execution mechanics). REMAINING: Mitri's feel-test of all four voices — do they sound like four different people? Mitri is the judge. NOTE: Mitri's framing June 4 — it's not about the $1K, it's changing lives + businesses that need us; mission = every employee a real Level-4 agent with real feel.
4. **⤴️ ORDER AMENDED June 4 late (Mitri's call, CEO concurs — overrides the eve pivot): dev proceeds with ONBOARDING INCR 2+3 NOW.** Reason: the email build is blocked on MITRI's domain purchase anyway (dev can't wire DNS he doesn't have) — so dev builds onboarding while Mitri buys the domain in parallel; both clocks start today. **ACCEPTANCE TEST: Mitri re-onboards FOR REAL at /onboarding as Surge (customer #1)** — Atlas researches the landing page + interviews him about Surge; confirmed answers become the live memory_entries every employee reads. No simulation — real data, real DB, real feel-test. If it doesn't feel right to Mitri, no customer sees it. Email wiring begins the moment domain credentials land.
   **Domain + email channel (was: active build; now: starts on credentials):** buy domain, wire Resend/Gmail API, SPF/DKIM/DMARC, warmup per aria.md. Unlocks: sending (revenue), replies (reactive triggers). PIVOT REASON: warmup is wall-clock time (weeks) that can't be compressed — start the slowest clock first; onboarding incr 2 has no clock. **MITRI ACTION: buy a SEPARATE sending domain (never cold-email from the main domain — e.g. trysurge.io-style sibling) + mailbox/Resend account, hand dev credentials.** Dev wires DNS/API; warmup starts same day.
   - ✅ **June 4 eve: onboarding-v2 INCREMENT 1 SHIPPED + PUSHED (dcf0c68)** — intake mode in lib/chat-prompt.mjs (toolsFor(), intakeTools sealed from normal chat, server-gated complete_onboarding), verify-onboarding-v2 green + regressions green (chat-prompt 4/4, atlas 16/16). Live behavior verified: website-first, single question, refuses to fabricate. **🔐 GIT RISK CLOSED: first push to origin/main confirmed; remote matches working tree (swept up MEMORY/landing/personas/prompt-doc edits).**
   - ✅ **June 4: onboarding-v2 INCREMENT 2 SHIPPED (backend lifecycle)** — `supabase/onboarding_migration.sql` (companies.onboarding_complete flag + created_at + research_findings jsonb; form fields made nullable for DRAFT rows; backfill existing = complete; brand-assets private bucket + per-company-path storage RLS). Chat route (`app/api/chat`) now runs intake: intakeMode get-or-creates the draft, `toolsFor({intakeMode})`, injects persisted research findings, executes save_company_profile (partial) + extended remember_detail kinds + **server-gated complete_onboarding** (re-checks the required set in the DB, refuses early). New `app/api/onboard/research` (auth-gated, Haiku web_search → emit_findings, logs usage_log, ONE paid call/company, cached after). Shared gate `isRequiredSetMet`/`REQUIRED_INTAKE_TYPES` in chat-prompt.mjs (no drift). verify-onboarding-v2 extended + green (gate + anon-blocked storage); regressions green; tsc clean. **MITRI MUST RUN the migration in Supabase SQL Editor (Run without RLS) — code errors against the old schema until then.** Not yet committed-push status: see git. ⚠️ live app still uses the OLD form until incr 3.
   - ✅ **June 4: onboarding-v2 INCREMENT 3 SHIPPED (UI front door)** — `/onboarding` is now Atlas's intake CONVERSATION (`components/IntakeChat.tsx`): website-first (auto-calls /api/onboard/research before the turn so Atlas presents findings), logo upload → brand-assets, completion detected via isOnboardingComplete → redirect to /dashboard. Old 7-question form DELETED (app/onboarding/page.tsx rewritten: branded left panel + IntakeChat). Front-door: dashboard centerpiece `components/AtlasBrief.tsx` (auto-streams Atlas's morning brief once/session via sessionStorage guard + his always-on input that routes through Atlas); Ctrl+K CommandBar now DEFAULTS to Atlas ('aria,'/'nova,'/'opus,' prefixes still route direct); Atlas added to EmployeeAvatar (amber) + ChatPanel suggestions. tsc + eslint + `next build` all green (18 pages). Fixed a pre-existing dashboard setState-in-effect lint error in passing.
   - ✅ **June 5: MIGRATION CONFIRMED APPLIED + FULL E2E VERIFIED LIVE** against the running dev server. New `scripts/e2e-onboarding-v2.mjs` signs up a throwaway user, mints real @supabase/ssr cookies (browser-equivalent), and exercises the REAL routes: anon blocked (307), bad URL → 400, /api/onboard/research returns real structured findings from a live site + auto-creates the draft (onboarding_complete=false) + persists findings; completion gate REFUSES early (flag stays false, Atlas honest) and FLIPS true once icp/offer/voice/goal exist (+completed_at stamped); Atlas closes with the exact spec line. Hardened research route: strips web_search `<cite>` markup from findings (was intermittently leaking) + plain-text instruction. NOTE: email confirmation is OFF in this Supabase (signup → immediate session); e2e leaves throwaway qa-*@surge-qa.test auth users (company rows cleaned via cascade, anon-invisible). **REMAINING: only Mitri's subjective feel-test of the live UI click-through — the functional path is proven end-to-end.**
   - ✅ **June 5: POST-ACCEPTANCE POLISH PASS SHIPPED (6 items, all committed + pushed).** (1) Pricing centralized in `lib/pricing.mjs` (injectPricing for personas/prompts); killed the scattered "$299"/"$399"/"$999" literals incl. two stale $299 in aria.md; grep-proof clean. (2) Profile memory UPSERTs — re-confirming icp/offer/voice/goal/brand-kit updates the one row instead of duplicating; `supabase/dedupe_profile_memory.sql` cleans existing dupes (**Mitri: run it once** to clear his duplicate Offer). (3) Honest empty states on dashboard + briefing — every metric from real company-scoped data, zero shows as honest zero, Workforce Score shows "—" / "unlocks after your first week" (no mock 86/$184K); grep-proof: no mock stat constants. (4) Voice rule in shared chat-prompt rules — max ONE em-dash/reply, short sentences, specifics over filler (verify fails 3+ em-dashes; live: Aria 0). (5) Atlas "works the problem" — reads board+memory before asking, never re-asks what the brain knows, names why-now, surfaces contradictions (verify scenario: ICP-vs-GTM conflict → he names both, no stock question). (6) ICP reconciliation = behavioral (Atlas now surfaces it). Also fixed 6 pre-existing lint errors (setState-in-effect, Date.now-in-render, unescaped entities) so the whole tree lints green. **GATES: tsc + eslint(app/components/lib) + `next build`(18) green; verify-{chat-prompt,atlas,onboarding-v2,usage-limits,leads,chat,auth} + live e2e all green.**
   - ⚠️ **ICP RECONCILIATION (Mitri action):** onboarding ICP ("high-ticket earners, kept broad") is broader than the GTM ICP on file (med spas / real estate / gyms, North Houston→Conroe). Atlas will now surface this in chat/brief; reconcile it with him when you have a minute.
   - ✅ **June 5: HOURS METERING SHIPPED (the invisible workday quota is DEAD).** One visible currency = HOURS of employee time (never credits/coins/tokens; top-up = "overtime"). Chat is free; WORK costs hours; thin/failed actions charge ZERO. Config in `lib/pricing.mjs` (HOUR_PRICES: research_scan 0.5 / aria_run 2 / nova_content 1 / nova_video 12 / opus_brief 1; ALLOWANCES single 70 / team 200; OVERTIME_PACKS $25→20h, $99→100h; pegging rule = ≥70% margin from usage_log telemetry, re-derive monthly). Enforcement helper `lib/hours.mjs` (gateWork → run → debitHours). `supabase/hours_migration.sql`: hours_ledger (+RLS) + hours_balance/hours_append fns + grant-on-creation trigger + backfill + companies.plan. /api/agent/run + /api/onboard/research gate on balance then debit actual (0 on thin/fail). Research route also got the polish item-6 fixes folded in: direct page-fetch first then web_search, thin-scan cache bug fixed (only meaningful scans persist+charge), anti-loop daily backstop, no-invented-internals honesty rule. UI: dashboard fuel-gauge (`HoursWidget`), settings Timesheet + real plan card (killed stale $897/"Unlimited tasks"), Atlas low-balance overtime nudge via chief context, landing rewritten to hours language ("200 hours… ~100 prospecting runs", overtime anytime; killed "unlimited tasks"/"full workday"). Quota system deleted (PLAN_QUOTAS/quotaForPlan/capacityMessage/consume_task_run gone from code; usage_log COST TELEMETRY stays as the pricing source; verify-usage-limits repurposed to telemetry+pricing). New `scripts/verify-hours.mjs` + `scripts/grant-hours.mjs` (dev top-ups until Stripe). **GATES: tsc + eslint + `next build`(18) green; verify-{hours(config+grep),usage-limits,chat-prompt,atlas,onboarding-v2,leads,chat,auth} green.** ⚠️ **MITRI: run `supabase/hours_migration.sql` (Run without RLS).** Until then the metered routes (task run + onboarding research) error, and verify-hours' LIVE checks + e2e research are SKIPPED/pending. After you run it, I'll run verify-hours live to confirm the gate/debit/RLS end-to-end. Stripe overtime SKUs map to OVERTIME_PACKS when billing ships.
   - 🛠️ **June 5: EMAIL CHANNEL SCAFFOLDING PRE-BUILT (credential-free, switch-on-ready).** Domain decided: **getsurgehq.com** (dedicated sending domain; mailbox aria@getsurgehq.com via Google Workspace; Resend = sending engine). Built ahead so go-live is a same-hour flip + the warmup clock (the long pole) starts immediately. `lib/email.mjs`: `sendEmail()` gated on RESEND_API_KEY → physical_address (CAN-SPAM hard gate) → suppression list → warmup cap; sends via Resend REST API (no SDK dep) with List-Unsubscribe one-click headers; warmup ramp 10→100/day over ~31 days; CAN-SPAM footer (address + unsubscribe). `supabase/email_migration.sql`: companies.physical_address + warmup_started_at; email_sends (+unsub_token, RLS); email_suppressions (RLS); email_unsubscribe(token) SECURITY DEFINER for anon one-click. `app/api/unsubscribe` (GET confirm + POST one-click, honored automatically). Settings: CAN-SPAM address editor (outbound blocked until set). `scripts/verify-email.mjs` green (safe-by-default, warmup monotonic, footer compliant, wiring present; live DNS/send SKIP pending creds). **NOT yet wired into Aria's loop (no send tool in the run) — that + the migration are the deliberate switch-on.** **TO GO LIVE (Mitri): create Workspace + Resend, add SPF/DKIM/DMARC + MX DNS in Namecheap, put RESEND_API_KEY in .env.local, run email_migration.sql, set physical_address in Settings. Then dev wires Aria's send_email tool + verify-email live + warmup starts.** OPEN Q for Mitri: confirm getsurgehq.com is a DEDICATED sending sibling, not the primary brand domain.
   - ✅ **June 5: AUTOMATIC MEMORY CAPTURE (the moat now compounds on its own).** Mitri's ask: every conversation with every agent should auto-save the key pieces. Before, the brain only grew when the model chose to call remember_detail (hit-or-miss). Now after EVERY chat turn (all employees, normal chat — skipped during intake which has its own save flow), a cheap Haiku pass (`lib/memory-extract.mjs`) reads the exchange and extracts only DURABLE facts (icp/offer/proof/voice/goal/brand-kit/process/decision/open-loop/idea/rapport/note), deduped against existing titles, conservative (small talk → nothing). Runs server-side AFTER the stream closes so it never delays the reply; best-effort (failure can't break chat). Persisted via a shared `persistMemory()` (refactored out of remember_detail) so profile types still UPSERT — no duplicates. Chat stays FREE (not metered) — this is the retention/moat engine. **Verified live (`scripts/verify-memory.mjs`): one fact-rich turn auto-saved ICP (dental/Austin) + banned-word (synergy) + goal (20 demos) with NO remember_detail call; a pleasantry saved nothing.** tsc + eslint + build green. Also this session: fixed dashboard Atlas to chat INLINE (was navigating to /workforce); added Memory delete (deleteMemoryEntry + UI, RLS-scoped) — both verified.
   - ✅ **June 5: CATCH-UP — both pre-billing HARD GATES closed (dev-doable, no creds/migrations needed).** (A) **Real Workforce Performance Score** ("The One Number"): `lib/score.mjs` transparent v1 formula from REAL data only — Execution 40% (task completion), Pipeline 35% (qualified+ leads), Momentum 25% (leads+drafts last 7d); weights renormalize over touched lanes; **score=null until there's real work** (no fake number). `getWorkforceScore()` in data.ts; dashboard shows live ScoreRing + breakdown (or honest empty state), briefing banner shows real score or "—". `scripts/verify-score.mjs` green. The mock 86 is gone for good. (B) **Legal/trust surface**: `/legal/terms` + `/legal/privacy` (accurate to what the product does — backs the landing's "encrypted, isolated, never trains others": Supabase storage + RLS isolation, Anthropic API not used for training, CAN-SPAM unsubscribe honored). Linked from landing footer + signup acceptance line. Public route group (layout + AppShell + proxy PUBLIC_PATHS). **Also fixed a real bug: `/api/unsubscribe` was proxy-gated → anon recipients got redirected to login; added it to PUBLIC_PATHS (verified 200 anon).** ⚠️ Mitri/counsel: review the policies + confirm the legal entity name/registered address before charging; governing law defaulted to Texas, contact support@getsurgehq.com. tsc + eslint + build (legal pages static) green.
   - ✅ **June 5: LEVEL-4 PROACTIVE HEARTBEAT (the "work happened while you slept" engine).** Enforces Aria's Lead Lifeline automatically. `lib/heartbeat.mjs`: pure `planSweep(leads, now)` (overdue active leads → resurface; overdue AND 90+ days → recycle) + `applySweep(supabase, companyId)` (recycles stale leads, creates ONE idempotent "Re-engage N overdue leads" recovery task for Aria, logs an activity summary) — works with any client. TWO triggers: (1) **`/api/heartbeat`** (authed) fired by the **dashboard once/day** (localStorage date guard) so the sweep runs the moment the owner opens the app — LIVE NOW, no creds; refreshes tasks/activity/score if it changed anything. (2) **`/api/cron/heartbeat`** (the while-you-sleep, all-companies version) — service-role client + CRON_SECRET guard, **dormant until both are set** (mirrors email scaffolding). `scripts/verify-heartbeat.mjs` green: planSweep unit cases + LIVE authed sweep (overdue→recovery task, 90-day lead recycled, second run idempotent — no duplicate). tsc + eslint + build green. ⚠️ **TO ENABLE the nightly cron (Mitri): set `SUPABASE_SERVICE_ROLE_KEY` + `CRON_SECRET` in .env.local and point a daily scheduler (Vercel/Supabase cron) at POST /api/cron/heartbeat with `Authorization: Bearer <CRON_SECRET>`.** (The per-visit authed sweep already covers the single-user case today.) NOT YET: Aria's autonomous daily prospecting cycle + Atlas's brief-by-email (the latter waits on the email channel) — heartbeat is the scaffold they'll plug into.
   - ✅ **June 5: ARIA'S STANDING DAILY CYCLE (safe, no autonomous spend).** Extended the heartbeat sweep: when Aria is IDLE (no open queued/in-progress task) AND the brain has a directive (an `icp` or `goal` memory entry), it queues her a 'Daily Prospecting' task. Naturally idempotent (once she has an open task, no second one) and DEFERS behind re-engaging overdue leads (recovery task takes priority). Crucially it only QUEUES — execution still goes through Run + the hours gate, so there is NO autonomous spend yet (auto-execution is the post-hours-migration switch-on). Also fixed a loose end from the heartbeat: the leads page counted `recycled` leads as overdue — now excluded. `scripts/verify-heartbeat.mjs` extended + green (idle+ICP → daily task; idempotent; deferred while recovery task open). tsc + eslint + build green. So the heartbeat now does: recycle stale → re-engage overdue → queue daily prospecting, all idempotent, all surfaced.
   - 🔌 **June 5: EMAIL SENDING WIRED (Resend live — RESEND_API_KEY + EMAIL_FROM=aria@getsurgehq.com in .env.local; domain getsurgehq.com VERIFYING in Resend).** Send is connected to the APPROVAL flow (CEO mandate: nothing sends without owner approval). `lib/email.mjs` `deliverDraft()` (load draft+lead → sendEmail → on success mark draft 'sent' + lead 'contacted' w/ day-3 follow-up). `/api/leads/approve` (authed): approve → mark approved + deliverDraft (returns sent/queued + plain-English reason); reject → mark rejected. Leads page: "Approve & send" button → server route, shows Sent ✓ / queued-reason. Warmup DRIP: heartbeat `applySweep` drains approved drafts via deliverDraft up to the daily warmup cap (10/day day1 → 100 by ~day31, per WARMUP_SCHEDULE); cap hit → stops for the day; suppressed → terminally rejected; first send sets warmup_started_at. lead_drafts.approval_status lifecycle: pending → approved → sent (or rejected). `verify-email` structural checks green (deliverDraft/approve/heartbeat wiring); tsc+eslint+build green. **⚠️ 3 GATES before a real email goes out (Mitri): (1) run `supabase/email_migration.sql` (email_sends/email_suppressions/physical_address/unsubscribe fn — NOT applied yet, I verified the tables are missing); (2) set the business mailing address in Settings → Sending & Compliance (CAN-SPAM hard gate, blocks send until set); (3) Resend domain verification must finish (currently "verifying" — until then Resend rejects sends and deliverDraft logs them failed, draft stays approved to retry). After all 3: approving a draft sends, warmup starts at 10/day. Until then, approvals queue safely (status 'approved', no spend, clear reason shown).**
   - ✅ **June 5: EMAIL LIVE — first send inbox-delivered, warmup day 1.** (Mitri ran email_migration.sql + set the mailing address + Resend domain verified.)
   - 🚀 **June 5: LAUNCH PREP — ALL mock stats killed (item 3 broadened; the surgehq.io/Vercel launch blocker).** Earlier item-3 cleaned dashboard+briefing; this pass killed the REMAINING fabricated stats a visitor/customer sees: the **Workforce list + employee detail** were still rendering seeded `performanceScore` rings, fake `uptime`/`tasksToday`/`currentTask`/`kpis`-with-`change%`, and the **dashboard workforce panel** showed the seeded `currentTask`. New `getEmployeeStats()` in data.ts computes REAL per-employee figures from this company's tasks/leads/drafts (active/completed counts, real current task or "Idle", Aria's real Leads/Qualified/Emails-Sent KPIs; other employees get honest Active/Completed; zeros render dimmed). Removed both per-employee score rings (the company-wide Score lives on the dashboard). Grep-proof: no `performanceScore`/`tasksToday`/`uptime`/`.change` render sites left; `mockData` is types-only (no display data anywhere). tsc+eslint+build green. **Net: every visitor-facing number is now real or an honest zero/empty state — safe to deploy.**
   - ✅ **June 5: CONFIRM-EMAIL flow wired (Mitri flipping the Supabase toggle back ON).** Signup already shows "Check your email" when no session returns; added `emailRedirectTo=/auth/confirm?next=/onboarding` + new `app/auth/confirm/route.ts` (verifyOtp(token_hash,type) → sets session → redirect to onboarding; failure → /login?error=confirm) + `/auth/confirm` in proxy PUBLIC_PATHS. **MITRI (Supabase dashboard): (1) Auth → flip "Confirm email" ON; (2) set Site URL = https://surgehq.io; (3) add redirect allowlist: https://surgehq.io/** and http://localhost:3000/**; (4) edit the "Confirm signup" email template link to `{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email&next=/onboarding`.** NOTE: app domain = surgehq.io (Vercel); sending domain = getsurgehq.com (Resend) — intentionally separate.
   - ✅ **June 5: NIGHTLY CRON STAGED + DEPLOY PREP.** `vercel.json` committed: daily **13:00 UTC GET → /api/cron/heartbeat** (active, not commented — warmup needs daily consistency; per-visit sweep stalls the ramp). **Fixed: Vercel cron invokes with GET but the route only exported POST → added `export const GET = runCron` alongside POST (shared handler, same CRON_SECRET auth).** Dormant until `CRON_SECRET` + `SUPABASE_SERVICE_ROLE_KEY` land in Vercel env (returns 503 cron_not_configured). Also fixed unsubscribe/confirm base-URL default → APP domain (surgehq.io), not the getsurgehq.com parking page (CAN-SPAM: every sent email's unsubscribe link builds from NEXT_PUBLIC_APP_URL — **critical Vercel env var**). `DEPLOY.md` runbook committed. Green light: tsc + build (25 routes) + all 12 verify scripts pass. **LOCKED LAUNCH ORDER: honest dashboard ✅ → deploy → Cowork DNS → run verify suite against PROD → re-enable Confirm email → THEN lift the send-freeze on the 7 drafts.** Vercel env (Mitri pastes in dashboard, never chat): NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, ANTHROPIC_API_KEY, RESEND_API_KEY, EMAIL_FROM, NEXT_PUBLIC_APP_URL=https://surgehq.io, + CRON_SECRET & SUPABASE_SERVICE_ROLE_KEY when ready.
   - ✅ **June 5: ARIA PROSPECTS THE CUSTOMER'S REAL ICP (multi-tenant correctness fix).** `/api/agent/run` was hardcoded to "focus on MED SPAS in North Houston up to Conroe" for EVERY customer — invisible for Mitri (his GTM matches) but it would have ignored customer #2's ICP entirely. Now RULE 2 injects a `targetBrief` built from the brain: the company's `target_customers` + saved `icp` memory entries (fallback: infer from the profile + say so). Genericized the log_activity example too. (hours + email migrations confirmed applied — metered runs live.) tsc + build green; grep-proof: no hardcoded vertical in app/lib; behavioral spot-check PASS (given a dentist/Austin ICP, Aria targets dentists, not med spas). NOTE: the gold-standard gate is a real web-search run with a non-med-spa test company — recommended post-deploy (costs hours + ~5 min; client-side undici 5-min timeout caveat applies, use node:http if scripted).
   - ✅ **June 4: MIGRATION RUN (Cowork ran it in Supabase SQL Editor with Mitri's approval — "Success. No rows returned") + incr-3 UI verified live in browser:** /onboarding shows the Atlas intake (website-first opener, logo clip, 5-step rail), dashboard shows Atlas Morning Brief centerpiece + Ask-Atlas bar, old form gone. Mitri's earlier "stale" onboarding click was pre-migration. Note: dev server running on port **3000** on Mitri's machine this session (QC Pro not running, Next took 3000). **NOW IN PROGRESS: the acceptance test — Mitri re-onboards as Surge, customer #1.**
   - ✅ **June 5: ACCEPTANCE TEST RUN — onboarding WORKS end to end.** Mitri re-onboarded as Surge; brain verified in /memory: ICP (high-ticket business owners using AI to optimize — intentionally open, no disqualifiers yet), Offer ($399/agent, $999/team), Brand Voice (confident/direct; banned: synergy, leverage-as-verb, game-changer, revolutionary, cutting-edge), 90-Day Goal (20 paying customers). Server gate flipped onboarding_complete; dashboard greets "Surge HQ", 3 online. Findings: (1) 🐛 FIXED LIVE by Cowork (`app/memory/page.tsx`): /memory crashed on new intake types (typeLabels[type].slice on undefined) — added colorFor/labelFor fallbacks + tags null-guard; verified rendering. NOT yet committed — dev should sweep into next commit. (2) 🐛 DEV FIX QUEUED: intake saved DUPLICATE Offer entries (draft "Transformation TBD" + final) — save_company_profile should upsert per (company_id, type), not append. (3) ⚠️ ESCALATED: dashboard shows FAKE stats on a fresh company ($184K revenue influenced, 28 meetings, 94 leads, 47h saved, score 86 "+4 pts") — same never-invent-metrics violation as briefing page; a real new customer would see invented results on day one. Must be real/zeroed before billing (build item 8). (4) Note: Mitri's confirmed ICP here ("high-ticket earners, kept open") is BROADER than the GTM ICP on file (med spas/real estate/gyms, North Houston→Conroe) — reconcile before Aria's first real send. Caveat flagged: Surge has no public domain yet, so Atlas's site research has nothing public to scan (localhost isn't reachable by the research engine) — expect the honest "scan came up short" path unless Mitri points him at a deployed page; answers then come from Mitri directly.
5. **Onboarding v2 — "Atlas runs the intake"** — 📤 **build prompt handed off June 4: `prompts/onboarding-v2-atlas-intake-prompt.md`** (intake flow + brand kit + Atlas front-door UI + /api/onboard/research + verify-onboarding-v2.mjs; ships BEHIND email channel, never instead). Design (Mitri + CEO, June 4; replaces the 7-question form; NON-NEGOTIABLE before charging — CEO-mandate failure point #1): signup → conversation with Atlas, not a form. Flow: (a) ask for website URL → system researches it with the EXISTING web research engine → Atlas opens "I read your site — here's what I understand, correct me"; (b) structured interview writes typed entries to the memory layer (ICP, brand voice, offer, proof points, differentiators, goals) — this is HOW all employees personalize; (c) **brand kit capture** in the same conversation: logo upload OR "I'll grab it from your site" + colors + banned words → Supabase storage + memory. PUSHBACK LOGGED: logo CREATION rejected (feature-factory trap, doesn't move outreach; a missing logo becomes a Nova brief, not a product feature). Note: media = Nova's lane (not Opus).
6. **Atlas front-door UI (Mitri + CEO, June 4):** Atlas is not one card among four — dashboard centerpiece = his Morning Brief, plus his own persistent input docked on every page (the existing Ctrl+K command bar becomes Atlas's bar). Default: talk to the company through Atlas; he routes. HARD BOUNDARY: direct chat with every employee STAYS — Atlas never gatekeeps.
7. **Level 4 proactive layer** — scheduled heartbeat: Aria's standing daily cycle + Lead Lifeline overdue sweep + Atlas automated Morning Brief (his v2 spec). The "work happened while you slept" moment — the real $1K justification.
8. **Honest retention surfaces** — briefing page on real activity_log/KPIs + honest empty states (flagged bug), real Workforce Performance Score (the One Number is currently mock — and the team plan now advertises it, so it must be real before billing).
9. **Opus's first real action: handoff briefs** (auto one-pager per booked meeting — the "my AI team sent me this" moment) + **Nova's execution runner** (content deliverables + approval flow, same pattern as Aria's).
   **📐 NOVA SPEC EXPANDED (Mitri + CEO, June 5):** (a) **Nova Studio page** — her surface, like /leads is Aria's: deliverables library (type, status draft→approved→published, asset, caption, platform). Every employee gets a home: Aria=/leads, Atlas=dashboard+brief, Nova=/studio, Opus=briefs on meetings (his lane activates when Aria books). (b) **Video = Higgsfield via API, NEVER browser-puppeting or customer-facing plugins** — server calls the API, file lands in Supabase storage → Studio. Customer never sees/touches Higgsfield (same invisibility rule as Resend behind Aria). Cost identical to using their site; reliability and compute far better. Rent generation engines via API, never rebuild, never puppet. (c) **Publishing phases:** P1 download+caption in Studio (customer posts, human click = approval promise); P2 official APIs — YouTube Data API, Meta Graph, TikTok — Approve→posts (LinkedIn stays manual-paste: no API, ToS hunts bots); P3 scheduled autonomous per content calendar. (d) Hours pricing: video ≈ 12h incl regens, from telemetry.
10. **Legal/trust surface (CEO flag, June 4 — HARD GATE alongside billing):** Terms of Service + Privacy Policy + data-processing language must exist BEFORE first paying customer (landing page already promises "encrypted, isolated, never trains others" with no policy behind it — credibility + legal risk). **CAN-SPAM enforcement in the SYSTEM before first send:** physical postal address in footer, working unsubscribe honored automatically (not just persona-promised), honest subjects. ~A day of work with standard SaaS templates; cheap insurance, mandatory.
11. **Stripe billing** — $999 team / $399 single. Then: first paying customer.

## Design Direction (June 3, 2026)
- Current landing/app look is too basic for the vision. Decision: redesign via **v0.app (free tier)** — outputs Next.js+Tailwind (our stack), Mitri iterates visually himself, dev integrates the winner. Adobe XD rejected (discontinued/maintenance mode, mockup-only). Lovable = backup, Figma Make = exploration only.
- CEO wrote the v0 design brief (light theme, indigo, Apple × Palantir × Tesla, employee cards, score section, $299 pricing vs $50K human framing). Mitri runs v0 in parallel — does NOT touch the dev critical path (auth → HQ Chat).
- ✅ **June 3: v0 landing INTEGRATED & VERIFIED by dev** — `app/landing/page.tsx` (single-file v0 design), shadcn Button + cn util recreated, globals.css tokens appended additively (internal pages untouched), framer-motion/lucide/cva/clsx/tailwind-merge/radix-slot installed. Build green, /landing public + 200, Geist via next/font (zero serif), zero fabricated claims (grep-proven). All CTAs → /signup; Contact sales → mailto placeholder `CONTACT_SALES_MAILTO` constant (currently hello@surge.app — NOT a real address yet, swap when domain/email exists). Mobile = v0's responsive classes as-approved, no device pass done yet.
- **June 3: Mitri APPROVED the v0 landing design** (8 sections: hero / team cards / how-it-works / score ring / coming-soon / pricing w/ dark Growth card / FAQ / final CTA). CEO review required 4 fixes before ship: Geist Sans everywhere (was rendering serif), indigo #6366f1 primary CTAs (was all black), HONESTY PASS (remove fake "2,400+ businesses", fake "SOC 2 certified", Aria "closes deals"→"books qualified meetings" — fabricated trust claims NEVER ship), raw &apos; bug. Integration prompt handed to dev: v0 ZIP → replaces /landing, CTAs → /signup, /landing stays public in proxy.ts.

## Recurring
- **Daily meeting scheduled — 8:00 AM every day** (Cowork scheduled task `surge-daily-meeting`): reads CLAUDE.md/MEMORY.md/CEO.md, delivers status → working → at-risk → next 3 moves.

## Business Model
- $299/month per AI employee
- Basic: 1 employee | Growth: 3 employees | Enterprise: unlimited + custom training
- Goal: First paying customer ASAP

## Key Files
- Mock data: `lib/mockData.ts`
- Global styles + CSS vars: `app/globals.css`
- Sidebar: `components/Sidebar.tsx`
- All pages: `app/dashboard/`, `app/workforce/`, `app/workforce/[id]/`, `app/tasks/`, `app/memory/`
